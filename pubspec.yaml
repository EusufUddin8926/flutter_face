name: flutter_facerecognization
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.8.1
  google_ml_kit: ^0.7.0
  path_provider: ^2.0.15
  tflite_flutter: ^0.9.0
  image: ^3.0.2
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/mobilefacenet.tflite
